<!DOCTYPE html>
<html>
<% include partials/head %>

<body>
  <% include partials/nav %>


  <div class="section no-pad-bot" id="index-banner">
    <div class="container">
      <br><br>
      <div class="row header header-border">
        <div class="col s12 light">
      <h4>New Contract</h4>
    </div>
  </div>
      <div class="row">
        <div class="col s9 light">

          <!--new contract form-->
          <!-- add a text field to create a new client? -->

          <form action="/contracts/added" method="post" class="grey lighten-3 header table-padding header-left">

            <div class="row">
              <div class="input-field col s7">
                <input id="name" type="text" name="target_name">
                <label for="name">Target Name</label>
              </div>

              <div class="input-field col s4">
                <input id="seclevel" type="number" name="target_security">
                <label for="seclevel">Security Level</label>
              </div>
            </div>

            <div class="row">
              <div class="input-field col s7">
                <input id="location" type="text" name="target_location">
                <label for="location">Target Location</label>
              </div>

              <div class="input-field col s4">
                <input id="budget" type="number" name="budget">
                <label for="budget">Budget</label>
              </div>
            </div>

            <div class="row">
            <div class="input-field col s7">
              <input id="t-image" type="url" name="target_photo">
              <label for="t-image">Target Image URL</label>
            </div>
          </div>

            <div class="row">
              <div class="input-field col s7">
                <select id="clientvalselect" name="clients">
                <option value="" disabled selected>Choose Client</option>

                <% for (var i=0; i<results.length; i++) { %>
                  <option name="client_name" value="<%= results[i].client_name %>"><%= results[i].client_name %></option>
                <% } %>
              </select>
                <label>Client</label>
                 <input type="hidden" name="client_name" id="client_name" />

              </div>
            </div>
            <div class="row">
              <button class="btn waves-effect waves-light" type="submit" name="add">Add Contract</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

  <br><br>

  <% include partials/footer %>

  </body>

  </html>
